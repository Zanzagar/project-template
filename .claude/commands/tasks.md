List Taskmaster tasks for the current or specified tag.

Usage: `/tasks` or `/tasks <tag-name>`

If no tag specified, uses current tag from `.taskmaster/state.json`.

Execute:
```bash
task-master list --tag $ARGUMENTS --with-subtasks
```
