{
  "description": "Multi-model API configuration for /multi-plan and /multi-execute",
  "note": "Add these to your .env file — NEVER commit actual keys",
  "required_env_vars": {
    "GOOGLE_AI_KEY": {
      "description": "Google AI Studio API key for Gemini",
      "source": "https://makersuite.google.com/app/apikey",
      "subscription": "$20/month Google AI plan or free tier (limited)"
    },
    "OPENAI_API_KEY": {
      "description": "OpenAI API key for Codex/GPT",
      "source": "https://platform.openai.com/api-keys",
      "subscription": "$20/month ChatGPT Plus or pay-as-you-go API credits"
    }
  },
  "example_env": [
    "# Add to .env (already in .gitignore)",
    "GOOGLE_AI_KEY=your_google_ai_key_here",
    "OPENAI_API_KEY=your_openai_api_key_here"
  ],
  "verification": {
    "gemini": "curl -s 'https://generativelanguage.googleapis.com/v1/models?key=$GOOGLE_AI_KEY' | head -5",
    "openai": "curl -s -H 'Authorization: Bearer $OPENAI_API_KEY' https://api.openai.com/v1/models | head -5"
  },
  "graceful_degradation": "Both keys are optional. Commands work with Claude alone if keys are missing."
}
