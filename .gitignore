# Python
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
.venv/
env/
.env
*.egg-info/
dist/
build/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Jupyter
.ipynb_checkpoints/

# Project specific
*.log
*.tmp

# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific

# Plugins - installed plugins are user-specific
.claude/plugins/installed/
.claude/plugins/installed.json

# Claude Code logs from hooks
.claude/logs/

# Session summaries (ephemeral, per-user)
.claude/sessions/*.md

# Instincts (personal learned patterns, not shared by default)
.claude/instincts/*.json
!.claude/instincts/example.json
!.claude/instincts/export-*.json

# AgentShield scan marker
.claude/agentshield-last-run

# Generated project index (created by project-index.sh hook)
.claude/project-index.json

# Orchestration pipeline outputs (transient, per-run)
.claude/orchestrate/

# Multi-model plan outputs (transient, per-task)
.claude/plan/

# Project-specific MCP configuration (user preference)
.claude/mcp-project.json

# Local settings override (personal, not committed)
# NOTE: Shared hook config goes in .claude/settings.json (tracked).
#       Only use settings.local.json for personal overrides.
.claude/settings.local.json

# Template testing directory
test-projects/

# Template tracking (created by sync-template.sh in target projects)
.template/

# Task files
# tasks.json
# tasks/ 
